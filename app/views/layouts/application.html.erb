<!DOCTYPE html>
<html>
<head>
  <title>MembersOnly</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="prose mx-auto bg-gray-300">
  <nav class="flex justify-between items-center bg-gray-800 text-white p-4">
    <% if user_signed_in? %>
      <%= link_to "Profile", edit_user_registration_path, class: "hover:underline text-white"  %>
      <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "hover:underline" %>
      <%= link_to "Create a Post", new_post_path, class: "hover:underline text-white"  %>
    <% else %>
      <%= link_to "Log In", new_user_session_path, class: "hover:underline text-white" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "hover:underline text-white" %>
    <% end %>
  </nav>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <%= yield %>
</body>
</html>
